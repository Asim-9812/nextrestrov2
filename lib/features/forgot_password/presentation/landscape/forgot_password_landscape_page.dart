import 'package:flutter/material.dart';

class ForgotPasswordLandscapePage extends StatelessWidget {
  const ForgotPasswordLandscapePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Forgot Password (Landscape View Coming Soon)',
          style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
        ),
      ),
    );
  }
}
